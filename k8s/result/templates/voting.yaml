
# ----------------------------
# ConfigMap
# ----------------------------
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.result.service.name }}-config
data:
  POSTGRES_HOST: "{{ .Values.result.postgres.host }}"
  POSTGRES_PORT: "{{ .Values.result.postgres.port }}"
  POSTGRES_DB: "{{ .Values.result.postgres.db }}"

# ----------------------------
# Secret
# ----------------------------
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.result.service.name }}-secret
type: Opaque
stringData:
  POSTGRES_USER: "{{ .Values.result.postgres.user }}"
  POSTGRES_PASSWORD: "{{ .Values.result.postgres.password }}"

# ----------------------------
#  Deployment
# ----------------------------
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.result.service.name }}
spec:
  replicas: {{ .Values.result.service.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.result.service.selector }}
  template:
    metadata:
      labels:
        app: {{ .Values.result.service.selector }}
    spec:
      containers:
        - name: {{ .Values.result.service.name }}
          image: "{{ .Values.result.image.repository }}:{{ .Values.result.image.tag }}"
          imagePullPolicy: {{ .Values.result.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.result.service.port }}
          env:
            - name: POSTGRES_HOST
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.result.service.name }}-config
                  key: POSTGRES_HOST
            - name: POSTGRES_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.result.service.name }}-config
                  key: POSTGRES_PORT
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.result.service.name }}-config
                  key: POSTGRES_DB
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.result.service.name }}-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.result.service.name }}-secret
                  key: POSTGRES_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.result.service.name }}
spec:
  selector:
    app: {{ .Values.result.service.selector }}
    version: stable
  ports:
    - protocol: TCP
      port: {{ .Values.result.service.port }}
      targetPort: {{ .Values.result.service.port }}
  type: {{ .Values.result.service.type }}
